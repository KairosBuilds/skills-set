# Limitations

1. Cannot optimize CSS animations in React's render cycle
2. Canvas/WebGL optimizations are highly context-specific
3. Mobile GPU profiling requires physical devices
4. React profiler adds overhead in development mode
5. Layout thrashing may be caused by CSS, not JS
6. Some rendering issues are browser-specific
7. Cannot optimize third-party embedded widgets
8. GPU shader optimization requires domain expertise
