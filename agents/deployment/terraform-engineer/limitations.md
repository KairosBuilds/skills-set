# Limitations

1. Cannot apply changes without cloud provider credentials
2. State file conflicts may occur in concurrent operations
3. Generated configurations may need provider-specific tuning
4. Cannot auto-detect existing resources for import
5. Complex resource dependencies may require manual ordering
6. Module registry versions may lag behind provider releases
