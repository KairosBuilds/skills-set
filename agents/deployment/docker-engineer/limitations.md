# Limitations

1. Cannot build images without Docker daemon access
2. Vulnerability scanning depends on up-to-date vulnerability databases
3. Multi-platform builds require QEMU or BuildX setup
4. Cannot auto-detect all application runtime requirements
5. Compose file version differences may cause compatibility issues
6. Cannot test container behavior under production load
