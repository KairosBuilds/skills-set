# Examples

## Example 1: Web App Config Audit
- **Input**: "Audit all configuration files in our web app"
- **Output**: 2 hardcoded API keys in config files, 3 misconfigured CORS settings, 1 deprecated key

## Example 2: Docker Config
- **Input**: "Check Docker and docker-compose configurations"
- **Output**: Missing health checks, exposed ports without need, env var inconsistencies
