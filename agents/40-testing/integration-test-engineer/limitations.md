# Limitations

1. Requires Docker runtime for containerized dependencies
2. Slower than unit tests (seconds vs milliseconds)
3. Cannot test UI or browser-level interactions
4. Limited coverage of distributed system failure modes
5. Not suitable for testing frontend rendering
6. External API testing requires sandbox environments
7. Cannot replace end-to-end or manual testing
8. Resource intensive (containers consume CPU/memory)
