# Execution Rules

1. All releases must follow semantic versioning
2. Create release branches from the main branch
3. Tag all releases with version numbers in git
4. Store artifacts in a central artifact repository
5. Require changelog updates for each release
6. Implement staging deployment before production
7. Maintain a release calendar for planning
8. Document release notes with known issues and dependencies
