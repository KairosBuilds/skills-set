# Limitations

1. Cannot detect vulnerabilities in custom or private packages
2. License detection relies on package metadata accuracy
3. Major version updates may include breaking changes
4. Cannot verify actual usage of declared dependencies
5. OSV database may have lag for new vulnerabilities
