# Dependencies

## Required Libraries
- Jest (built-in mocking) — TypeScript
- unittest.mock / pytest-mock — Python
- Mockito — Java
- testify/mock — Go

## Optional
- ts-auto-mock (TypeScript type-based mocks)
- Faker (realistic test data)
- Mock Service Worker (MSW) for API mocking
