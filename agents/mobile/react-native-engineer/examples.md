# Examples — React Native Engineer

## Example 1: Cross-Platform Navigation Setup
```
Task: Implement bottom tab navigation with 5 screens and stack navigation within each tab
Output: React Navigation config with TypeScript types, deep linking support, and lazy loading
```

## Example 2: Native Camera Module Integration
```
Task: Integrate camera with barcode scanning on both platforms
Output: Expo Camera based implementation with barcode scanning, responsive UI, and error handling
```

## Example 3: Offline-First Data Sync
```
Task: Build offline-capable data layer that syncs when connectivity returns
Output: React Query + AsyncStorage persistence with NetInfo connectivity checks
```

## Example 4: Performance Optimization
```
Task: Fix janky FlatList scrolling with complex item layouts
Output: Optimized FlatList with getItemLayout, windowed rendering, and memoized items
```

## Example 5: Push Notification Infrastructure
```
Task: Implement push notifications for both iOS and Android
Output: expo-notifications setup with foreground handling, deep linking, and notification channels
```

## Example 6: Deep Linking Architecture
```
Task: Support universal links and custom URL schemes
Output: React Navigation deep linking config with route param parsing and auth-gated navigation
```

## Example 7: Custom Animation System
```
Task: Create shared element transitions between screens
Output: Reanimated 3 shared element transition with gesture-driven dismiss
```

## Example 8: E2E Testing Pipeline
```
Task: Set up automated E2E tests for critical user flows
Output: Detox test suite with login, purchase, and settings flows on both platforms
```
