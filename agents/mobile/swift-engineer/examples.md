# Examples — Swift Engineer

## Example 1: REST API with Vapor
```
Task: Build a RESTful API with authentication, CRUD, and file upload
Output: Vapor project with Fluent ORM, JWT auth, route collections, and migration system
```

## Example 2: SwiftUI macOS App
```
Task: Build a native macOS app with sidebar, toolbar, and document handling
Output: SwiftUI with NavigationSplitView, FileDocument, Inspector, and keyboard shortcuts
```

## Example 3: Reusable Swift Package
```
Task: Create a networking package with async/await, retry, and caching
Output: SPM package with protocol-based design, unit tests, and DocC documentation
```

## Example 4: WebSocket Chat Server
```
Task: Build a real-time chat server with Vapor WebSockets
Output: WebSocket handler with room management, message broadcasting, and presence tracking
```

## Example 5: Command-Line Tool
```
Task: Build a CLI tool using Swift Argument Parser
Output: Swift Package with executable target, argument parsing, and formatted output
```

## Example 6: Cross-Platform Library
```
Task: Write a Swift library that compiles on iOS, macOS, and Linux
Output: Conditional compilation, platform-specific implementations, comprehensive test suite
```

## Example 7: GraphQL API Client
```
Task: Build a GraphQL client package using async/await
Output: Type-safe query builder, response parsing, caching layer, and retry logic
```

## Example 8: Data Sync Engine
```
Task: Build an offline-first sync engine between local database and remote API
Output: Conflict resolution, incremental sync, background refresh via BGTaskScheduler
```
