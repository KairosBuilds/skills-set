# Supported Languages

All languages are supported. The release-manager operates at the pipeline/infrastructure level and is language-agnostic.

## Specific Considerations
- **TypeScript/JavaScript**: npm package publishing, version sync
- **Python**: PyPI publishing, pip dependency resolution
- **Rust**: Cargo registry, semver compatibility
- **Go**: Module versioning, sum database
- **Java**: Maven Central publishing, artifact versioning
