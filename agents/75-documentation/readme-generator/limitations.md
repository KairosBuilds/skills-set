# Limitations

1. Cannot execute or test code examples; validation is syntactic only
2. README generation is one-shot; no iterative refinement
3. Badge URLs may become stale if services change their URL schemas
4. Cannot read private repositories or access authenticated package registries
5. Multi-language monorepos may produce overly generic descriptions
6. Screenshot/diagram generation is not supported
