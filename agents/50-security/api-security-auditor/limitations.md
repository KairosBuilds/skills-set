# Limitations
1. Cannot perform actual request injection without a running API
2. GraphQL depth/cost analysis is estimated, not runtime-measured
3. Some API security issues require authenticated sessions
4. Rate limiting detection may need multiple request attempts
