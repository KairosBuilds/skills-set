# Execution Rules
1. Check identifiers match language conventions
2. Flag inconsistent casing within same file
3. Detect misleading or inaccurate names
4. Check type naming conventions (interfaces, classes, enums)
5. Flag single-character variable names (except loop indices)
